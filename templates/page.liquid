<section class="vossa-section bg-bone" style="padding-top: 120px;">
  <div class="vossa-section-inner" style="max-width:780px;">
    <h1 style="margin-bottom:32px;">{{ page.title }}</h1>
    <div style="font-size:16px;line-height:1.7;color:var(--ink);">{{ page.content }}</div>
  </div>
</section>
